# Diff: user-manual.adoc
# Similarity: 99.6%
# --- Asciidoctor (expected)
# +++ AdocNet (actual)

  </dd>
  </dl>
  <blockquote>
- Everything fails all the time.
+ <p>Everything fails all the time.</p>
  </blockquote>
  -- Werner Vogels
  <p>This is why DataSync writes an <em>audit log</em> of every change it applies,
  ...
