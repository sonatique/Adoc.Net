# AdocNet v1.0.0 Readiness Assessment

**Date**: 2026-04-21 (post-v1.0.0-beta.26)
**Status**: Technically very close, practically a few non-code steps away.

This document captures the honest current-state assessment and a concrete
roadmap to the first non-beta release. Author it once, revisit before each
RC tag.

## Executive summary

The codebase is essentially v1.0.0-quality already. What's missing is not
code — it's external validation, broader compatibility testing on real
corpora, and the commitment to API stability that the version number itself
implies.

Estimated total effort: **~3 days of code + a calendar window for feedback**.

## What's strong (no blockers here)

| Area | Status |
|---|---|
| Test coverage | 2907 tests, 0 regressions, growing every release |
| Format coverage | HTML, PDF, EPUB, DocBook, Reveal.js, Man — all 6 formats covered with structural diff tooling and Tier 1+2 parity fixes applied |
| Architecture | Extensions, caching, hot-reload, LSP server, Avalonia preview, CLI tools, themes — most of what Asciidoctor offers |
| Code quality | Deterministic output, comprehensive XML docs, no dead code, performance benchmarks |
| Cross-platform | CI green on Windows / Linux / macOS |
| Determinism | Output is byte-identical across runs and platforms (PDF since beta.25, HTML/EPUB/DocBook since beta.26) |

## What blocks v1.0.0

### 1. Compatibility tested against ONE document — PARTIALLY DONE

Started post-beta.26 with `tools/parity-sweep.py` running all diff
tools across `spec/conformance/asciidoctor-*.adoc` (5 representative
asciidoctor-style documents covering admonitions, document header,
lists, macros, tables).

**Initial sweep surfaced these gaps** (all fixed):

| Gap | Format | Where |
|---|---|---|
| Missing `<div id="preamble">` wrapper | HTML | content before first section |
| Footer "Last updated" missing timestamp | HTML | localdatetime fallback |
| Missing `docdatetime`/`doctime` parser defaults | HTML | parser |
| Sidebar block `id` attribute | HTML, DocBook | `<div class="sidebarblock">`, `<sidebar>` |
| Timezone `+01:00` vs `+0100` | HTML | parser |
| Inter-doc xref: `<olink>` → `<link xlink:href>` | DocBook | `xref:foo.adoc` |
| `.adoc` extension → `.xml` in xref label | DocBook | label fallback |
| `<formalpara>` xml:id not propagated | DocBook | titled verbatim blocks |
| `<screen>`/`<programlisting>` `linenumbering` attr | DocBook | callout + language paths |
| Admonition `<para>` → `<simpara>` | DocBook | inline-only content rule |

**Initial state across a 5-doc subset** (asciidoctor-* prefix, easy
ones — NOT representative of the corpus):

| format | min | median | max | sum |
|---|---|---|---|---|
| html | 0 | 0 | 0 | 0 |
| docbook | 0 | 11 | 42 | 92 |
| revealjs | 233 | 370 | 631 | 1949 |
| epub-struct | 1 | 1 | 1 | 5 |

**Reality check across the FULL spec/conformance/ corpus (36 docs)**:

The 5-doc subset above is the easy subset — files I prefix-named
`asciidoctor-*` from asciidoctor's own conformance examples. Running
the sweep against all 36 spec/conformance/*.adoc reveals the actual
state:

- HTML: 9/36 docs at 0 (was claiming 5/5 = 100% when it was really 9/36 = 25%)
- DocBook: 17/36 docs at 0
- Worst HTML offenders:
  - `mixed-features` 339 lines
  - `spring-security-auth` 254
  - `user-manual` 193
  - `technical-guide` 190
  - `api-reference` 158
- Recurring root causes (each affects many docs):
  - `<body class="... toc-left toc2">` — TOC layout class missing
  - `<div class="details">` — author byline / revision info block in
    header (when author line is set)
  - `<div class="toc2" id="toc">` inside header — TOC positioning for
    `:toc: left` / `:toc: right` (currently we render TOC differently)
  - `[tabs]` block style not recognised
  - `.listingblock primary` / `.listingblock secondary` classes for
    tab-style listings

**These are real v1.0.0 blockers** and weren't visible from the
asciidoctor-* subset because those docs don't use TOC layout, author
lines, or [tabs].

The 226 spec/fixtures/*.adoc files are smaller per-feature tests and
likely don't add many new gaps once the recurring root causes above
are fixed.

Per-document HTML / DocBook breakdown:

| document                    | html | docbook | reason for residual |
|-----------------------------|------|---------|---------------------|
| asciidoctor-document-header | 0    | 0       | byte-identical      |
| asciidoctor-macros          | 0    | 0       | byte-identical      |
| asciidoctor-lists           | 0    | 11      | `<quote>` element wrapping |
| asciidoctor-admonitions     | 0    | 39      | fixture noise (empty arearefs) |
| asciidoctor-tables          | 0    | 42      | `[%checklist]` styling |

**Remaining work to fully close gap 1**:

a. **HTML**: ✅ DONE. All 5 conformance docs at zero diff lines.
   Smart-quote substitution now applied to link/xref labels.

b. **DocBook**: most docs at 0 or low residual. Three categories left:
   - `<quote>` element wrapping for `"text"` (asciidoctor's
     `[quote]` inline replacement) — separate feature, ~2-3 hours.
   - `[%checklist]` style: `mark="none"` attribute + leading
     `✓` checkmark stripping — ~half day to implement properly.
   - Admonitions fixture noise: asciidoctor's empty `arearefs=""`
     because the conformance fixture stripped callout markers —
     not a real renderer bug.

c. **Reveal.js**: 233-631 lines per doc — substantial structural gaps:
   asciidoctor-revealjs uses table-based admonition layout, puts
   preamble inside title slide, and many block wrappers differ. This
   is the only format where a real refactor is needed. ~1 day.

d. **EPUB structural**: ✅ TRIAGED. The "1 common-part diff per doc"
   is `META-INF/container.xml` differing only in the path it points
   to (`EPUB/package.opf` vs `OEBPS/content.opf`) — both valid EPUB
   conventions, intentionally different. Not a real bug.

e. **Real-world projects** — still untested:
   - Asciidoctor user manual
   - Spring Boot reference docs
   - At least one book-doctype document

   **Effort for remaining**: ~1-2 days (Reveal.js refactor + DocBook
   `<quote>` element + real-world testing).

### 2. No external users yet

Per the historical context: no users, no forks, no watchers. v1.0.0
implies API stability promises — hard to make that promise without
2-3 outside users hitting the API and finding the rough edges.

**Approach**: tag `v1.0.0-rc.1` with the API frozen, actively solicit
real-world testing for 2-3 weeks. This is calendar time, not work time.

### 3. API surface not formally locked

Every beta has been making additive changes. v1.0.0 should declare:
"this is the supported API; changes here are SemVer breaking."

**Concrete deliverable**: audit public types in
- `AdocNet.Core` (AdocEngine, RenderOptions, IDocumentRenderer)
- `AdocNet.Parser` (AdocParser, ParseOptions, ParseResult)
- `AdocNet.Ast` (DocumentNode, all node types)
- Each renderer's options class (HtmlRenderOptions, PdfRenderOptions,
  EpubRenderOptions, DocBookRenderOptions, RevealjsRenderOptions,
  ManRenderOptions)
- Extension interfaces (IDocumentProcessor, IBlockProcessor,
  IInlineProcessor, IOutputProcessor, IExtensionLifecycle, etc.)

Mark each member as **stable** or apply `[Experimental]` if not yet
ready to commit. Anything experimental needs to either get promoted
or removed before v1.0.0.

**Effort**: half day.

### 4. Migration documentation

A "Moving from Asciidoctor to AdocNet" guide. One page with:

- CLI flag mapping table (asciidoctor flag → adocnet flag)
- Attribute support matrix (what works, what's planned, what's not)
- Output format compatibility notes (e.g. "DocBook output is
  byte-identical to asciidoctor for HOWTO.adoc with file mtime
  injection enabled")
- Known issues / intentional differences (e.g. EPUB doesn't bundle
  fonts by default; default theme differs from Asciidoctor's iconic
  red-on-cream — use `--theme asciidoctor` for that)
- Extension migration: AdocNet extensions are .NET assemblies, not
  Ruby gems. Asciidoctor extensions are NOT supported.

**Effort**: half day.

## What does NOT block v1.0.0

These are documented Tier 3 deferrals from the beta.26 sweep — real
or perceived gaps that we've decided are acceptable:

| Item | Reason |
|---|---|
| Man Tier 3 (`.sp` / `.RS` / `.RE` reformat) | AdocNet's `.PP` / `.IP` / `.TP` style produces functionally equivalent output via groff. AdocNet's dialect is actually more portable to mandoc on BSD. |
| EPUB font bundling | Adds ~640 KB per EPUB; opt-in trade-off documented. Default web-safe stack is sufficient for most readers. |
| DocBook `<date>` mtime fallback | Already done (beta.26). Honors `:reproducible:` opt-out. |
| Asciidoctor theme sub-pixel drift | Closed in beta.26 via DevTools-protocol diff. Three remaining cosmetic deltas (pre color, font-family, text-rendering) don't affect layout. |
| Per-format Tier 3 wrappers | Documented in respective `docs/*-PARITY-FIRSTPASS.md` files. None are correctness issues. |

## Concrete roadmap to v1.0.0

| Step | Status | Notes |
|---|---|---|
| 1. Sweep diff tools against `spec/conformance/*.adoc` and 2 real-world projects, fix the new gaps | ✅ Done (corpus only; real-world projects pending) | HTML 35/36 docs at 0 lines (only `[tabs]` inner-class propagation in spring-security-auth left). DocBook 31/36 at 0; total residual 280 lines down from 813 at session start. |
| 2. API stability audit; mark `[Experimental]` on anything not ready | ✅ Done | Verdict: lock as-is. ~120 public types reviewed; none flagged as experimental, none for removal. Immutable design throughout, clean interfaces. |
| 3. Migration guide doc (CLI flag mapping, attribute matrix, known issues) | ✅ Done | See `docs/MIGRATING-FROM-ASCIIDOCTOR.md`. |
| 4. Tag `v1.0.0-rc.1`, sit on it 2-3 weeks soliciting feedback | ⏳ Pending | Calendar work, not coding. |
| 5. Address feedback (likely small fixes) | ⏳ Pending | After rc.1 is out. |
| 6. Tag `v1.0.0` | ⏳ Pending | After feedback window closes. |

### Remaining DocBook gaps (acknowledged, not blockers)

| doc | residual | reason |
|---|---|---|
| `spring-security-auth` | 206 | `[tabs]` inner role= propagation, `javadoc:` macro, `WARNING:` paragraph admonition |
| `asciidoctor-admonitions` | 39 | Fixture noise — empty `arearefs=""` because conformance fixture stripped callout markers |
| `api-reference` | 13 | colspec width sum-to-100 quirk (asciidoctor adds residual to last column) |
| `asciidoctor-lists` | 11 | Smart curly-quote `"text"` should wrap in `<quote>` element in DocBook |
| `asciidoctor-tables` | 11 | Apostrophe smart-quote (letter`'`letter → letter`'`letter) not applied uniformly |

### Visual-review-only gaps (caught by side-by-side PNG inspection, not by structural diff tools)

| Format | Issue | Status |
|---|---|---|
| EPUB | Reader error "Opening and ending tag mismatch: col line ... and colgroup" — HTML void elements (`<col>`, `<br>`, `<img>`, `<hr>`, `<meta>`, `<link>`) emitted in non-XHTML form | ✅ Fixed (post-process to self-close in EpubRenderer) |
| HTML `--theme asciidoctor` | "version " was lowercase; should be "Version " (en.yml: `version-label: Version`) | ✅ Fixed |
| HTML `--theme asciidoctor` | `.details` block (author + version) appeared after the title's horizontal rule on its own lines instead of as a flex row above the rule | ✅ Fixed (asciidoctor-css `.details` flex layout + `<br>:before` separators added) |
| HTML `--theme asciidoctor` | `:toc: left` rendered the TOC inline in body content instead of as a fixed left sidebar | ✅ Fixed (asciidoctor-css `body.toc2 #toc.toc2` rules added) |
| HTML default + asciidoctor themes | `[horizontal]` description list rendered as bordered spreadsheet table; should be compact two-column borderless layout | ✅ Fixed (`.hdlist` CSS rules added) |
| PDF | Description-list items with source-block continuation (`Java::\n+\n[source,java]\n----\n…\n----`) emitted only the term — body content was dropped | ✅ Fixed (`RenderDescriptionList` now iterates `item.Children` for continuation blocks) |
| PDF | TOC entries lack page numbers and dot leaders | ⏳ Deferred to v1.x.minor — needs two-pass renderer architecture (single-pass writes TOC before sections register their page positions). Internal links remain clickable. |
| Reveal.js | AdocNet emits a dedicated full-page title slide; asciidoctor packs the title onto the first content slide | ⏳ Deferred to v1.x.minor — slide-composition refactor estimated at ~1 day in the original readiness assessment. |
| EPUB / HTML | Document title containing `: ` (e.g. "Feature Catalogue: AsciiDoc Stress Test") not split into title + subtitle in the chapter `<h1>`. DocBook does the split (since beta.27); HTML/EPUB still concatenate. | ⏳ Deferred to v1.x.minor — needs `<h1><span class="title">…</span><small class="subtitle">…</small></h1>` wrapper. |
| EPUB | Author block "Author Name" with avatar icon (rendered by asciidoctor-epub3 in chapter header) is omitted entirely | ⏳ Deferred to v1.x.minor — asciidoctor-epub3 injects `<header><div class="author">…</div></header>` in chapter XHTML. |
| EPUB | Inline `<div id="toc">…</div>` emitted in chapter content when `:toc: left` is set; asciidoctor-epub3 strips it (the EPUB reader provides its own ToC). | ⏳ Deferred to v1.x.minor — single-line skip in EpubRenderer chapter pipeline. |
| HTML / EPUB | Backtick-after-italic-close edge case: `_`italic mono`_` renders the closing backtick as literal text. Inline parser doesn't consume the trailing `` ` `` when adjacent to closing italic marker. | ⏳ Deferred to v1.x.minor — inline-parser edge case. |

## Possible quick wins worth considering before v1.0.0

These are nice-to-haves that came up during the beta.26 sweep but
weren't in scope:

- **Update README hero section** to mention the parity claim with
  concrete numbers ("byte-identical structural output to Asciidoctor
  for HTML, DocBook, Reveal.js; 33% pixel-diff for EPUB visual layout").
- **Showcase the `--theme asciidoctor` option** more prominently —
  it's the single biggest "I don't have to migrate my existing CSS"
  argument for adoption.
- **Asciidoctor compatibility test gate in CI** — currently the diff
  tools are dev-only. Adding a job that runs `html-diff.py` etc.
  against a small corpus and fails the PR if regression appears
  would prevent silent parity drift.
- **Performance comparison table** in README — AdocNet vs
  asciidoctor (Ruby) on the same corpus. We already have benchmarks;
  surfacing them publicly is a low-effort marketing win.

## When to revisit this document

- Before tagging `v1.0.0-rc.1` (verify steps 1-3 are done)
- Before tagging `v1.0.0` (verify rc feedback is addressed)
- After v1.0.0 ships (mark this document as historical, link to v2
  planning if applicable)
